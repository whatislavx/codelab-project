## Лабораторна робота №2: Верстка сайту "CodeLab" (EJS)

### Висновок

Під час виконання цієї лабораторної я зверстав каркас для сайту "CodeLab" за наданими макетами.

Робота не є набором статичних HTML-файлів, а являє собою невеликий, але повноцінний **веб-додаток на Node.js + Express + EJS**. Це дозволило мені виконати не тільки базові, але й усі додаткові та бонусні завдання.

---

### ✅ Виконання вимог

#### 1. Базові завдання (Семантика та структура)

* **Семантика:** Усі 6 сторінок (`index`, `courses`, `course-details`, `instructors`, `login`, `signup`) зверстані з використанням коректних тегів: `<header>`, `<footer>`, `<nav>`, `<main>`, `<section>`, `<article>` (для карток курсів) та `<aside>` (для фільтрів).
* **Форми:** На сторінках логіну та реєстрації використані теги `<form>` з `<label>`.
* **Адаптивність:** Для головної ілюстрації використано `<picture>`, а для всіх важливих зображень прописані `alt` атрибути.

#### 2. Додаткові та бонусні завдання (Головна частина роботи)

Головна проблема верстки — дублювання коду (постійне копіювання `<header>` та `<footer>`). Я вирішив цю проблему, як і вимагалось у бонусних завданнях, за допомогою **серверного шаблонізатора EJS**:

* **Викорінення дублювання:** `header` та `footer` винесені в окремі файли (`views/partials/`). Вони підключаються на всі сторінки автоматично (`<%- include(...) %>`).
* **Динамічні сторінки:** Замість 6+ однакових HTML-файлів для кожного курсу, я створив **один** шаблон `views/pages/course-details.ejs`. Сервер сам наповнює цей шаблон потрібними даними (назвою, описом, ціною) залежно від ID в URL (`/course/:id`).
* **Мікро-розмітка:** На цьому ж шаблоні (`course-details.ejs`) реалізована мікро-розмітка `schema.org` для типу `Course`.
* **Підготовка i18n:** На головній сторінці (`index.ejs`) додані атрибути `data-i18n-key="..."` для майбутніх перекладів.

---

### 🛠️ Стек та запуск

* **Технології:** Node.js, Express, EJS (Embedded JavaScript)
* **Запуск:**

1.  Встановити залежності:
    ```bash
    npm install
    ```
2.  Запустити локальний сервер:
    ```bash
    node server.js
    ```
3.  Відкрити в браузері [http://localhost:3000](http://localhost:3000).
